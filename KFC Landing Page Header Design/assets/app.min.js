"use strict"; const navigation = document.querySelector(".navigation"); document.querySelector(".toggle").onclick = function () { this.classList.toggle("active"), navigation.classList.toggle("active") }; const next = document.querySelector(".next"), prev = document.querySelector(".prev"), slidesImg = document.querySelector(".slides img"); let count = 0; const sliderFunc = function (count) { slidesImg.src = `./images/food-${count}.png` }; next.addEventListener("click", (function () { count = count >= 3 ? 0 : count + 1, sliderFunc(count) })), prev.addEventListener("click", (function () { count = count <= 0 ? 3 : count - 1, sliderFunc(count) }));